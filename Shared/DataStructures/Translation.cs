namespace Shared.DataStructures
{
    public class Translation
    {
        /// <summary>
        /// Update - Updates the current translation based on the parameters provided.
        /// </summary>
        /// <param name="deltaT">The time that has passed since the last frame was drawn.</param>
        /// <returns>True/False indicating that the translation has completed.</returns>
        public bool Update(float deltaT)
        {
            return true;
        }
    }
}
